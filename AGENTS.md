`ytmusicapi` is a Rust crate port of [`sigma67/ytmusicapi`](https://github.com/sigma67/ytmusicapi).
Use Serena for project context and Context7 for library/docs context.
Do not commit `docs/superpowers/specs/*.md` or `docs/superpowers/plans/*.md`; treat `docs/superpowers/` as local working material.
Treat `browser.json` as a secret. It is ignored by git and must not appear with real values in examples, logs, fixtures, or commits.
